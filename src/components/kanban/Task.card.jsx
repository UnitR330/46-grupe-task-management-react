import style from './Kanban.module.css';
import { Tag } from './Tag';

export function taskCard({title, description, deadline, tags}) {
    console.log(tags);



    // console.log(props.tag);
    // const tags  props.tags;
    // const {tags} = props;
}